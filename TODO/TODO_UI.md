# Goal, graphical test UI,

- a tool exposing all the implemented functionality in an user interface.


We have annotations now too.

have config file 
- with path to DIA-NN etc files
- with path where to store mudata anndata


- do we have a also a file which tells for wich proteobench module this data is?
We will need this information to be able to add the annotations for obs. 
   we should and we also set this path in the config
   Basically a file which helps us to map the mudata anndata with the annotation files in the proteobench module folder where we search for the module toml files which have the annotations.



